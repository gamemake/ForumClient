<?xml version="1.0" encoding="UTF-8"?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="ForumClient.PostCell" Orientation="Horizontal"
		HorizontalOptions="FillAndExpand"
		>
	<StackLayout BackgroundColor="Silver" x:Name="content" HorizontalOptions="FillAndExpand">
		<StackLayout Orientation="Horizontal" BackgroundColor="Gray" HorizontalOptions="FillAndExpand">
			<Label FontSize="10" Text="{Binding AuthorName}" HorizontalOptions="Start" />
			<Label FontSize="10" Text="" HorizontalOptions="CenterAndExpand" />
			<Label FontSize="10" Text="{Binding PostTime}" HorizontalOptions="End" />
		</StackLayout>
	</StackLayout>
</StackLayout>