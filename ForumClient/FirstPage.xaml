<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:ForumClient" x:Class="ForumClient.FirstPage" Title="Main" Padding="5,0">
	<ContentPage.Content>
		<ScrollView>
			<ListView x:Name="forumList" VerticalOptions="Start" ItemSelected="OnForumSelected" SeparatorVisibility="None">
				<ListView.ItemTemplate>
					<DataTemplate>
						<ViewCell>
							<StackLayout Spacing="0">
								<Label Text="{Binding Title}" VerticalTextAlignment="Center" FontAttributes="Bold" LineBreakMode="TailTruncation" VerticalOptions="CenterAndExpand" />
								<Label Text="{Binding Description}" FontSize="10" LineBreakMode="TailTruncation" VerticalOptions="End" />
							</StackLayout>
						</ViewCell>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</ScrollView>
	</ContentPage.Content>
</ContentPage>